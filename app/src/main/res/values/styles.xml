<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColor">#2d2c2c</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Common button styles -->
    <style name="AppBaseTheme.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">44dp</item>
        <item name="android:textSize">18dp</item>
    </style>

    <style name="AppBaseTheme.Button.Green">
        <item name="android:background">#0ac39e</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="AppBaseTheme.Button.Highlight">
        <item name="android:background">#4185f2</item>
        <item name="android:textColor">#ffffff</item>
    </style>
    
    <style name="style_day" parent="AppTheme">
        <item name="titlebarcolor">@color/colorPrimary</item>
        <item name="mainbackground">@color/note_item_background</item>
        <item name="contentbackground">@color/day_mode_content_background</item>
    </style>

    <style name="style_night" parent="AppTheme">
        <item name="titlebarcolor">@color/title_bar_night</item>
        <item name="mainbackground">@color/mian_background_night</item>
        <item name="contentbackground">@color/night_mode_content_background</item>
    </style>


</resources>
